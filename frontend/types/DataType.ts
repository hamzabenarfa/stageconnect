export interface OffreType{
    id: String,
   title:String,
   company:String,
   place:String,
   duration:String,
   paid:Boolean,
   img:String,
}

export interface EntrepriseType {
    id: string,
    nom: string,
    email?: string,
    location: string,
    adresse?: string,
    phone:string,
}

export interface OffreType{
    id: String,
   title:String,
   company:String,
   place:String,
   duration:String,
   paid:Boolean,
   img:String,
}

export interface UserType {
    id: string;
    nom: string;
    prenom: string;
    email: string;
    password: string;
  
    
  }
  