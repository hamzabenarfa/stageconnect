export interface OffreType{
    id: String,
   title:String,
   company:String,
   place:String,
   duration:String,
   paid:Boolean,
   img:String,
}

export interface EntrepriseType {
    id: string,
    nom: string,
    email: string,
    adresse: string,
}

export interface OffreType{
    id: String,
   title:String,
   company:String,
   place:String,
   duration:String,
   paid:Boolean,
   img:String,
}

export interface UserType {
    id: string;
    nom: string;
    prenom: string;
    email: string;
    password: string;
  
    
  }
  